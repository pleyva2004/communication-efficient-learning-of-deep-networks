# Python
__pycache__/
*.pyc
.venv/
venv/
.cache/
*.pt
*.bin

# OS
.DS_Store

# LaTeX byproducts (keep source.pdf — that's the paper, not a build artifact)
*.aux
*.log
*.bbl
*.blg
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
# Local root-level build outputs ONLY (anchored with a leading slash so they do
# NOT match pdfs/<same-name>.pdf at depth). The canonical compiled PDFs live under
# pdfs/, proofs/, and learning-map/tour.pdf and ARE committed by the render
# workflow — they must not be ignored, or CI's `git add` silently stages nothing
# (that unanchored ignore was the bug that left pdfs/ empty).
/04-literature-review.pdf
/05-improvements.pdf
/interview-handout.pdf
